<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>매치 목록 - KDT</title>

    <!-- Bootstrap & Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">

    <style>
        /* ===== 헤더용 공통 ===== */
        :root { --maxw: 1200px; }
        .logo{ display:inline-flex; align-items:center; color:#111; text-decoration:none; }
        .logo .eeung-logo{ width:150px; height:auto; display:block; }
        .logo.sm .eeung-logo{ width:120px; }
        .logo.lg .eeung-logo{ width:180px; }
        .site-header{ background:#e9f2ff; border-bottom:1px solid #cfe0ff; }
        .topbar{
            max-width:var(--maxw); margin:0 auto; height:60px;
            display:flex; align-items:center; justify-content:space-between; padding:0 20px;
        }
        .auth-links a{ margin-left:10px; text-decoration:none; color:#111; }
        .auth-links .sep{ color:#c3c3c3; margin:0 6px; }
        .sport-nav{ background:#fff; border-top:1px solid #e5eefc; }
        .sport-menu{ max-width:var(--maxw); margin:0 auto; display:flex; gap:20px; padding:12px 20px; }
        .sport-menu a{ text-decoration:none; color:#007bff; font-weight:bold; }



        /* ===== Cards / Page ===== */
        .match-card { transition: transform 0.2s; border: 1px solid #e0e0e0; }
        .match-card:hover { transform: translateY(-2px); box-shadow: 0 4px 8px rgba(0,0,0,0.1); }
        .venue-image { height: 200px; object-fit: cover; }
        .status-badge { position: absolute; top: 10px; right: 10px; }
        .price-info { color: #28a745; font-weight: bold; }

        /* ===== Footer ===== */
        .site-footer{ background:#e5e7eb; border-top:1px solid #d1d5db; margin-top:40px; }
        .footer-row{ display:flex; flex-wrap:wrap; align-items:center; gap:.5rem; margin-bottom:.5rem; }
        .site-footer a{ text-decoration:underline; color:#111827; }
    </style>
</head>

<body>
<!-- ===== Header (바디 가장 위) ===== -->
<header class="site-header">
    <div class="container">
        <div class="topbar">
            <a href="/" class="logo" aria-label="이응 로고">
                <!-- 로고 SVG (currentColor 기반) -->
                <svg class="eeung-logo" viewBox="0 0 240 80" role="img">
                    <circle cx="40" cy="40" r="22" fill="none" stroke="currentColor" stroke-width="12" stroke-linecap="round"/>
                    <circle cx="28" cy="20" r="6" fill="currentColor"/>
                    <text x="80" y="50" font-family="Pretendard,'Noto Sans KR',system-ui" font-weight="700" font-size="28" fill="currentColor">[ 이응 ]</text>
                </svg>
            </a>
            <div class="auth-links">
                {{#loggedInUser}}
                <a href="/users/edit">마이페이지</a><span class="sep">|</span>
                <a href="/logout">로그아웃</a>
                {{/loggedInUser}}
                {{^loggedInUser}}
                <a href="/login">로그인</a><span class="sep">|</span>
                <a href="/register">회원가입</a>
                {{/loggedInUser}}
            </div>
        </div>
    </div>
</header>

<!-- ===== Main ===== -->
<div class="container mt-4">
    <!-- 성공/에러 메시지 표시 -->
    {{#message}}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle me-2"></i>{{message}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {{/message}}

    {{#error}}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-triangle me-2"></i>{{error}}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {{/error}}

    <div class="row mb-4">
        <div class="col-md-6">
            <h2>매치 목록</h2>
            <p class="text-muted">참여 가능한 매치를 찾아보세요</p>
        </div>
        <div class="col-md-6 text-end">
            {{#loggedInUser}}
                <div class="d-flex align-items-center justify-content-end mb-2">
          <span class="text-success me-3">
            <i class="fas fa-user me-1"></i>{{loggedInUser.nickname}}님 환영합니다!
          </span>
<!--                    <form action="/logout" method="post" style="display: inline;">-->
<!--                        <button type="submit" class="btn btn-outline-danger btn-sm">-->
<!--                            <i class="fas fa-sign-out-alt"></i> 로그아웃-->
<!--                        </button>-->
<!--                    </form>-->
                </div>
            {{/loggedInUser}}


            <a href="/match/create" class="btn btn-success">
                <i class="fas fa-plus"></i> 매치 등록
            </a>
        </div>
    </div>

    <!-- 스포츠별 필터 -->
    <div class="row mb-4">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <div class="row g-2">
                        <div class="col-md-3">
                            <a href="/match/list" class="btn btn-outline-primary w-100 {{^sportId}}active{{/sportId}}">
                                🏆 전체
                            </a>
                        </div>
                        <div class="col-md-3">
                            <a href="/match/sport/1" class="btn btn-outline-primary w-100 {{#sportId}}active{{/sportId}}">
                                ⚽ 축구
                            </a>
                        </div>
                        <div class="col-md-3">
                            <a href="/match/sport/2" class="btn btn-outline-info w-100 {{#sportId}}active{{/sportId}}">
                                🥅 풋살
                            </a>
                        </div>
                        <div class="col-md-3">
                            <a href="/match/sport/3" class="btn btn-outline-danger w-100 {{#sportId}}active{{/sportId}}">
                                ⚾ 야구
                            </a>
                        </div>
                    </div>
                    <div class="row g-2 mt-2">
                        <div class="col-md-3">
                            <a href="/match/sport/4" class="btn w-100 {{#sportId}}active{{/sportId}}" style="background-color: #ff8c00; border-color: #ff8c00; color: #fff;">
                                🎾 테니스
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- 매치 목록 -->
    <div class="row" id="matchList">
        {{#matches}}
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card match-card h-100">
                    <div class="position-relative">
                        {{#venue.venueImage}}
                            <img src="{{venue.venueImage}}" class="card-img-top venue-image" alt="{{venue.venueName}}">
                        {{/venue.venueImage}}
                        {{^venue.venueImage}}
                            <div class="card-img-top venue-image bg-light d-flex align-items-center justify-content-center">
                                <i class="fas fa-map-marker-alt fa-3x text-muted"></i>
                            </div>
                        {{/venue.venueImage}}

                        <!-- 상태 배지 -->
                        <span class="badge status-badge
              {{#status.OPEN}}bg-success{{/status.OPEN}}
                            {{#status.FULL}}bg-warning{{/status.FULL}}
                            {{#status.CANCELLED}}bg-danger{{/status.CANCELLED}}
                            {{#status.COMPLETED}}bg-secondary{{/status.COMPLETED}}">
              {{#status.OPEN}}모집중{{/status.OPEN}}
                            {{#status.FULL}}마감{{/status.FULL}}
                            {{#status.CANCELLED}}취소{{/status.CANCELLED}}
                            {{#status.COMPLETED}}완료{{/status.COMPLETED}}
            </span>
                    </div>

                    <div class="card-body">
                        <h5 class="card-title text-primary">{{venue.venueName}}</h5>
                        <p class="card-text text-muted">{{sport.sportName}}</p>

                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="d-flex align-items-center mb-2">
                                    <i class="fas fa-calendar text-success me-2"></i>
                                    <strong>{{matchDate}}</strong>
                                </div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="d-flex align-items-center mb-2">
                                    <i class="fas fa-clock text-info me-2"></i>
                                    <strong>{{startTime}} - {{endTime}}</strong>
                                </div>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="d-flex align-items-center justify-content-between p-2 bg-light rounded">
                                    <div class="d-flex align-items-center">
                                        <i class="fas fa-users text-primary me-2"></i>
                                        <span class="fw-bold">참가자</span>
                                    </div>
                                    <div class="text-end">
                                        <span class="h5 mb-0 text-success">{{currentPlayers}}</span>
                                        <span class="text-muted">/{{maxPlayers}}명</span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="row mb-2">
                            <div class="col-12">
                                <small class="text-muted">
                                    <i class="fas fa-map-marker-alt"></i>
                                    {{#venue.region}}{{venue.region.regionName}}{{/venue.region}}{{^venue.region}}지역 정보 없음{{/venue.region}}
                                </small>
                            </div>
                        </div>

                        {{#description}}
                            <p class="card-text">{{description}}</p>
                        {{/description}}
                    </div>

                    <div class="card-footer">
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="/match/{{matchId}}" class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-info-circle"></i> 상세보기
                            </a>
                            {{#status.OPEN}}
                                <a href="/match/{{matchId}}/apply" class="btn btn-success btn-sm" >
                                    <i class="fas fa-plus"></i> 신청하기
                                </a>
                            {{/status.OPEN}}


                        </div>
                    </div>
                </div>
            </div>
        {{/matches}}

        {{^matches}}
            <div class="col-12">
                <div class="text-center py-5">
                    <i class="fas fa-futbol fa-3x text-muted mb-3"></i>
                    <h4 class="text-muted">참여 가능한 매치가 없습니다</h4>
                    <p class="text-muted">다른 조건으로 검색해보세요</p>
                </div>
            </div>
        {{/matches}}
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

<!-- ===== Footer (바디 끝, 스크립트 위/아래 상관없음) ===== -->
<footer class="site-footer">
    <div class="container py-3">
        <div class="footer-row">
        <span class="logo">
          <svg class="eeung-logo" viewBox="0 0 240 80" role="img">
            <circle cx="40" cy="40" r="22" fill="none" stroke="currentColor" stroke-width="12" stroke-linecap="round"/>
            <circle cx="28" cy="20" r="6" fill="currentColor"/>
            <text x="80" y="50" font-family="Pretendard,'Noto Sans KR',system-ui" font-weight="700" font-size="28" fill="currentColor">[ 이응 ]</text>
          </svg>
        </span>
            <strong>이옹(EEUNG)</strong>
        </div>
        <div class="footer-row">
            041-123-4567 |
            <a href="mailto:contact@eeung-sports.com">contact@eeung-sports.com</a> |
            충남 천안시 동남구 백석대학로 1 (백석대 스포츠센터 3층)
        </div>
        <div class="footer-row">
            SNS:
            <a href="#">Instagram</a>
            <a href="#">Facebook</a>
            <a href="#">YouTube</a>
            <span>| © 2025 EEUNG. All rights reserved.</span>
        </div>
    </div>
</footer>
</body>
</html>
