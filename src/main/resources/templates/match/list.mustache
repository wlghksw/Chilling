<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>매치 목록 - KDT</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .match-card {
            transition: transform 0.2s;
            border: 1px solid #e0e0e0;
        }
        .match-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .venue-image {
            height: 200px;
            object-fit: cover;
        }
        .status-badge {
            position: absolute;
            top: 10px;
            right: 10px;
        }
        .price-info {
            color: #28a745;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="row mb-4">
            <div class="col">
                <h2><i class="fas fa-futbol"></i> 매치 목록</h2>
                <p class="text-muted">참여 가능한 매치를 찾아보세요</p>
            </div>
        </div>

        <!-- 스포츠별 필터 -->
        <div class="row mb-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-3">
                                <a href="/match/list" class="btn btn-outline-primary w-100 {{^sportId}}active{{/sportId}}">
                                    <i class="fas fa-futbol me-2"></i>전체
                                </a>
                            </div>
                            <div class="col-md-3">
                                <a href="/match/sport/1" class="btn btn-outline-success w-100 {{#sportId}}active{{/sportId}}">
                                    <i class="fas fa-futbol me-2"></i>축구
                                </a>
                            </div>
                            <div class="col-md-3">
                                <a href="/match/sport/2" class="btn btn-outline-info w-100 {{#sportId}}active{{/sportId}}">
                                    <i class="fas fa-futbol me-2"></i>풋살
                                </a>
                            </div>
                            <div class="col-md-3">
                                <a href="/match/sport/3" class="btn btn-outline-warning w-100 {{#sportId}}active{{/sportId}}">
                                    <i class="fas fa-baseball-ball me-2"></i>야구
                                </a>
                            </div>
                        </div>
                        <div class="row mt-3">
                            <div class="col-md-3">
                                <a href="/match/sport/4" class="btn btn-outline-danger w-100 {{#sportId}}active{{/sportId}}">
                                    <i class="fas fa-table-tennis me-2"></i>테니스
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- 매치 목록 -->
        <div class="row" id="matchList">
            {{#matches}}
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card match-card h-100">
                    <div class="position-relative">
                        {{#venue.venueImage}}
                        <img src="{{venue.venueImage}}" class="card-img-top venue-image" alt="{{venue.venueName}}">
                        {{/venue.venueImage}}
                        {{^venue.venueImage}}
                        <div class="card-img-top venue-image bg-light d-flex align-items-center justify-content-center">
                            <i class="fas fa-map-marker-alt fa-3x text-muted"></i>
                        </div>
                        {{/venue.venueImage}}
                        
                        <!-- 상태 배지 -->
                        <span class="badge status-badge 
                            {{#status.OPEN}}bg-success{{/status.OPEN}}
                            {{#status.FULL}}bg-warning{{/status.FULL}}
                            {{#status.CANCELLED}}bg-danger{{/status.CANCELLED}}
                            {{#status.COMPLETED}}bg-secondary{{/status.COMPLETED}}">
                            {{#status.OPEN}}모집중{{/status.OPEN}}
                            {{#status.FULL}}마감{{/status.FULL}}
                            {{#status.CANCELLED}}취소{{/status.CANCELLED}}
                            {{#status.COMPLETED}}완료{{/status.COMPLETED}}
                        </span>
                    </div>
                    
                    <div class="card-body">
                        <h5 class="card-title text-primary">{{venue.venueName}}</h5>
                        <p class="card-text text-muted">{{sport.sportName}}</p>
                        
                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="d-flex align-items-center mb-2">
                                    <i class="fas fa-calendar text-success me-2"></i>
                                    <strong>{{matchDate}}</strong>
                                </div>
                            </div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="d-flex align-items-center mb-2">
                                    <i class="fas fa-clock text-info me-2"></i>
                                    <strong>{{startTime}} - {{endTime}}</strong>
                                </div>
                            </div>
                        </div>
                        
                        <div class="row mb-2">
                            <div class="col-6">
                                <small class="text-muted">
                                    <i class="fas fa-users"></i> {{currentPlayers}}/{{maxPlayers}}명
                                </small>
                            </div>
                            <div class="col-6">
                                <small class="text-muted">
                                    <i class="fas fa-map-marker-alt"></i> {{#venue.region}}{{venue.region.regionName}}{{/venue.region}}{{^venue.region}}지역 정보 없음{{/venue.region}}
                                </small>
                            </div>
                        </div>
                        
                        {{#description}}
                        <p class="card-text">{{description}}</p>
                        {{/description}}
                    </div>
                    
                    <div class="card-footer">
                        <div class="d-flex justify-content-between align-items-center">
                            <a href="/match/{{matchId}}" class="btn btn-outline-primary btn-sm">
                                <i class="fas fa-info-circle"></i> 상세보기
                            </a>
                            {{#status.OPEN}}
                            <a href="/match/{{matchId}}/apply" class="btn btn-success btn-sm">
                                <i class="fas fa-plus"></i> 신청하기
                            </a>
                            {{/status.OPEN}}
                        </div>
                    </div>
                </div>
            </div>
            {{/matches}}
            
            {{^matches}}
            <div class="col-12">
                <div class="text-center py-5">
                    <i class="fas fa-futbol fa-3x text-muted mb-3"></i>
                    <h4 class="text-muted">참여 가능한 매치가 없습니다</h4>
                    <p class="text-muted">다른 조건으로 검색해보세요</p>
                </div>
            </div>
            {{/matches}}
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

