<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>매치 신청</title>
    <style>
        body { 
            margin:0; 
            background:#1c1f22; 
            color:#fff; 
            font-family:system-ui, sans-serif; 
            padding: 20px;
        }
        h2 { text-align:center; margin:18px 0; font-weight:800; }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        /* 매치 정보 스타일 */
        .match-info {
            background: rgba(255,255,255,.05);
            padding: 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            border: 1px solid rgba(255,255,255,.1);
        }
        .match-info h3 {
            margin-top: 0;
            color: #0d6efd;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            padding: 8px 0;
            border-bottom: 1px solid rgba(255,255,255,.1);
        }
        .info-label {
            font-weight: 600;
            color: #ccc;
        }
        .info-value {
            color: #fff;
        }

        /* 신청 폼 스타일 */
        .application-form {
            background: rgba(255,255,255,.05);
            padding: 20px;
            border-radius: 12px;
            border: 1px solid rgba(255,255,255,.1);
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #fff;
        }
        .form-group input, .form-group select, .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid rgba(255,255,255,.2);
            border-radius: 8px;
            background: rgba(255,255,255,.1);
            color: #fff;
            font-size: 16px;
            box-sizing: border-box;
        }
        .form-group input:focus, .form-group select:focus, .form-group textarea:focus {
            outline: none;
            border-color: #0d6efd;
            box-shadow: 0 0 0 3px rgba(13, 110, 253, 0.25);
        }
        .btn-apply {
            width: 100%;
            padding: 15px;
            background: #0d6efd;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
        }
        .btn-apply:hover {
            background: #0b5ed7;
        }

        .btn-cancel {
            width: 100%;
            padding: 15px;
            background: #dc3545;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 10px;
        }
        .btn-cancel:hover {
            background: #c82333;
        }

        .btn-secondary {
            width: 100%;
            padding: 15px;
            background: #6c757d;
            color: #fff;
            border: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.2s;
            margin-top: 10px;
        }
        .btn-secondary:hover {
            background: #5a6268;
        }

        /* 스포츠 아이콘 */
        .sport-icon {
            font-size: 48px;
            text-align: center;
            margin-bottom: 20px;
        }

        /* 알림 메시지 */
        .notice {
            background: rgba(255, 193, 7, 0.1);
            border: 1px solid rgba(255, 193, 7, 0.3);
            color: #ffc107;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }

        /* 신청 상태 표시 */
        .application-status {
            background: rgba(40, 167, 69, 0.1);
            border: 1px solid rgba(40, 167, 69, 0.3);
            color: #28a745;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }

        .error-message {
            background: rgba(220, 53, 69, 0.1);
            border: 1px solid rgba(220, 53, 69, 0.3);
            color: #dc3545;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: center;
        }

        /* 디버깅용 스타일 */
        .debug-info {
            background: rgba(0, 123, 255, 0.1);
            border: 1px solid rgba(0, 123, 255, 0.3);
            color: #007bff;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            text-align: left;
            font-family: monospace;
            font-size: 12px;
        }

    </style>
</head>
<body>
<div class="container">
    <h2>{{match.sport.sportName}} 매치 신청</h2>

    <!-- 스포츠 아이콘 -->
    <div class="sport-icon">
        {{#match.sport.sportCode}}
            {{#match.sport.sportCode.equals("SOCCER")}}⚽{{/match.sport.sportCode.equals}}
            {{#match.sport.sportCode.equals("FUTSAL")}}⚽{{/match.sport.sportCode.equals}}
            {{#match.sport.sportCode.equals("BASEBALL")}}⚾{{/match.sport.sportCode.equals}}
            {{#match.sport.sportCode.equals("TENNIS")}}🎾{{/match.sport.sportCode.equals}}
            {{^match.sport.sportCode.equals}}🏃{{/match.sport.sportCode.equals}}
        {{/match.sport.sportCode}}
    </div>

    <!-- 매치 정보 -->
    <div class="match-info">
        <h3>매치 정보</h3>
        <div class="info-row">
            <span class="info-label">경기장:</span>
            <span class="info-value">{{match.venue.venueName}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">날짜:</span>
            <span class="info-value">{{match.matchDate}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">시간:</span>
            <span class="info-value">{{match.startTime}} - {{match.endTime}}</span>
        </div>
        <div class="info-row">
            <span class="info-label">참가자:</span>
            <span class="info-value">{{match.currentPlayers}}/{{match.maxPlayers}}명</span>
        </div>
        <div class="info-row">
            <span class="info-label">스포츠:</span>
            <span class="info-value">{{match.sport.sportName}}</span>
        </div>
    </div>

    <!-- 알림 메시지 -->
    <div class="notice">
        <strong>알림:</strong> 이 스포츠는 기본 신청 양식을 사용합니다. 
        스포츠별 맞춤 신청 양식을 원하시면 관리자에게 문의해주세요.
    </div>

    <!-- 에러 메시지 표시 -->
    {{#errorMessage}}
    <div class="error-message">
        <strong>오류:</strong> {{errorMessage}}
    </div>
    {{/errorMessage}}

    <!-- 성공 메시지 표시 -->
    {{#successMessage}}
    <div class="application-status">
        <strong>성공:</strong> {{successMessage}}
    </div>
    {{/successMessage}}

    <!-- 현재 사용자의 신청 상태에 따라 다른 내용 표시 -->
    {{#hasApplied}}
        <!-- 이미 신청한 경우: 신청 취소 옵션 -->
        <div class="application-form">
            <h3>매치 신청 현황</h3>
            <div class="application-status">
                <strong>이미 이 매치에 신청하셨습니다!</strong><br>
                신청 포지션: {{userApplication.position}}<br>
                신청 상태: {{userApplication.status}}<br>
                신청 시간: {{userApplication.appliedAt}}
            </div>
            <form action="/match/{{match.matchId}}/cancel" method="post">
                <button type="submit" class="btn-cancel" onclick="return confirm('정말로 매치 신청을 취소하시겠습니까?')">
                    매치 신청 취소하기
                </button>
            </form>
            <a href="/match/list" class="btn-secondary">다른 매치 보기</a>
        </div>
    {{/hasApplied}}
    {{^hasApplied}}
        <!-- 신청하지 않은 경우: 신청 폼 -->
        <div class="application-form">
            <h3>매치 신청</h3>
            <form action="/match/{{match.matchId}}/apply" method="post" onsubmit="return checkDuplicateApplication()">
                <div class="form-group">
                    <label for="position">희망 포지션</label>
                    <input type="text" id="position" name="position" placeholder="원하는 포지션을 입력하세요" required>
                </div>
                <div class="form-group">
                    <label for="price">참가비 (원)</label>
                    <input type="number" id="price" name="price" min="0" step="1000" placeholder="참가비를 입력하세요" required>
                </div>
                <div class="form-group">
                    <label for="message">메시지 (선택사항)</label>
                    <textarea id="message" name="message" rows="3" placeholder="매치 참가에 대한 메시지나 특별한 요청사항이 있다면 입력해주세요"></textarea>
                </div>
                <button type="submit" class="btn-apply">매치 신청하기</button>
            </form>
        </div>
    {{/hasApplied}}

</div>

<script>
function checkDuplicateApplication() {
    // hasApplied가 true인 경우 중복 신청 방지
    const hasApplied = {{#hasApplied}}true{{/hasApplied}}{{^hasApplied}}false{{/hasApplied}};
    
    if (hasApplied) {
        alert('이미 이 매치에 신청하셨습니다. 중복 신청은 불가능합니다.');
        return false;
    }
    
    return true;
}
</script>
</body>
</html>
