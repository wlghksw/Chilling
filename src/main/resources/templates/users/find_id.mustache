    <!DOCTYPE html>
    <html lang="ko">
    <head>
        <meta charset="UTF-8">
        <title>아이디 찾기</title>
        <style>
            body { text-align:center; font-family: Arial, sans-serif; }
            .box {
              display:inline-block; margin-top:40px; padding:24px; width:380px;
              border:1px solid #ddd; border-radius:12px; background:#f9f9f9;
            }
            label { display:block; text-align:left; margin-top:12px; font-size:14px; }
            input { width:100%; padding:10px; margin-top:8px; box-sizing:border-box; }
            button { width:100%; padding:12px; margin-top:14px; cursor:pointer; }
            .msg { margin-top:12px; }
            .msg.error { color:#d33; }
            .msg.success { color:#0a8; }
            .links { margin-top:14px; font-size:14px; }
            .links a { margin:0 6px; color:#007BFF; text-decoration:none; }
            .links a:hover { text-decoration:underline; }
        </style>
    </head>
    <body>

    <h2>아이디 찾기</h2>

    <div class="box">
        {{#error}}<p class="msg error">{{error}}</p>{{/error}}
        {{#success}}<p class="msg success">{{success}}</p>{{/success}}

        <form method="post" action="/users/find_id">
            <label>이름</label>
            <input type="text" name="realName" value="{{#realName}}{{.}}{{/realName}}" placeholder="실명 입력">

            <label>전화번호</label>
            <input type="text" name="phone" value="{{#phone}}{{.}}{{/phone}}" placeholder="예) 01012345678">

            <label>생년월일</label>
            <input type="text"
                   name="birthYear"
                   value="{{#birthYear}}{{.}}{{/birthYear}}"
                   placeholder="예) 20010727"
                   pattern="[0-9]{8}"
                   maxlength="8"
                   required>

            <button type="submit">아이디 찾기</button>
        </form>

        <div class="links">
            <a href="/users/login">로그인</a> |
            <a href="/users/signup">회원가입</a> |
            <a href="/users/find_pw">비밀번호 찾기</a>
        </div>
    </div>

    {{#loginId}}
        <script>
            alert("가입된 아이디: {{loginId}}");
            window.location.href = "/users/login";
        </script>
    {{/loginId}}

    </body>
    </html>
