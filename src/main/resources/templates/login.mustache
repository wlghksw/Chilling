<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <title>로그인</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>

    <style>
        /* ===== 헤더 공통 ===== */
        :root { --maxw: 1200px; }

        .logo{ display:inline-flex; align-items:center; color:#111; text-decoration:none; }
        .logo .eeung-logo{ width:120px; height:auto; display:block; }
        .logo.sm .eeung-logo{ width:100px; }
        .logo.lg .eeung-logo{ width:150px; }

        .site-header{ background:#e9f2ff; border-bottom:1px solid #cfe0ff; }
        .topbar{
            max-width:var(--maxw); margin:0 auto; height:60px;
            display:flex; align-items:center; justify-content:space-between; padding:0 20px;
        }
        .auth-links a{ margin-left:10px; text-decoration:none; color:#111; font-weight:600; }
        .auth-links .sep{ color:#c3c3c3; margin:0 6px; }
        .sport-nav{ background:#fff; border-top:1px solid #e5eefc; }
        .sport-menu{ max-width:var(--maxw); margin:0 auto; display:flex; gap:20px; padding:12px 20px; }
        .sport-menu a{ text-decoration:none; color:#007bff; font-weight:bold; }

        /* ===== 레이아웃 (footer 바닥 고정) ===== */
        html, body { height:100%; }
        body{
            min-height:100vh;
            display:flex;
            flex-direction:column; /* footer 하단 유지 핵심 */
            background:
                    radial-gradient(1000px 400px at 10% -10%, #d8efff, transparent 70%),
                    radial-gradient(800px 400px at 110% 10%, #e8f4ff, transparent 65%),
                    linear-gradient(180deg, #ffffff 0%, #f6fbff 100%);
            color:#111;
            font-family:'Pretendard','Noto Sans KR',system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
        }
        main{
            flex:1; /* 남는 공간 채워서 footer를 아래로 */
            display:flex;
            align-items:center;
            justify-content:center;
            padding:32px 16px;
        }

        /* ===== 로그인 카드 ===== */
        .login-card{
            width:100%;
            max-width:420px;
            background:#fff;
            border-radius:16px;
            border:1px solid rgba(47,151,244,.10);
            box-shadow:0 12px 32px rgba(0,77,153,.12);
            overflow:hidden;
        }
        .login-header{
            padding:28px 28px 20px;
            background:linear-gradient(180deg,#f3f9ff,#eaf5ff);
            border-bottom:1px solid rgba(88,175,255,.35);
            text-align:center;
        }
        .login-header h3{
            margin:0 0 6px;
            font-weight:800;
            color:#16324f;
            letter-spacing:-.2px;
        }
        .login-header p{ margin:0; color:#6b7a90; }

        .login-body{ padding:28px; }

        .form-label{ font-weight:700; color:#16324f; }
        .form-control{
            border-radius:10px;
            border:1px solid rgba(47,151,244,.25);
            padding:12px 14px;
            transition:border-color .2s ease, box-shadow .2s ease, transform .08s ease;
        }
        .form-control:focus{
            border-color:#58afff;
            box-shadow:0 0 0 .25rem rgba(88,175,255,.35);
        }

        /* input-group 아이콘 정렬용 살짝 다듬기 */
        .input-group-text{
            background:#f8fbff;
            border-color:rgba(47,151,244,.25);
            border-radius:10px 0 0 10px;
        }
        .input-group .form-control{
            border-left:0;
            border-radius:0 10px 10px 0;
        }

        .btn-login{
            border:0;
            border-radius:12px;
            padding:12px 14px;
            font-weight:800;
            letter-spacing:.2px;
            background:linear-gradient(135deg,#7fc3ff,#2f97f4);
            color:#06223b;
            transition:transform .08s ease, box-shadow .2s ease;
        }
        .btn-login:hover{
            transform:translateY(-1.5px);
            box-shadow:0 12px 24px rgba(88,175,255,.35);
            color:#06223b;
        }

        .links-row a{
            text-decoration:none;
            font-weight:700;
            color:#2f97f4;
        }
        .links-row a:hover{ color:#58afff; text-decoration:underline; }

        /* ===== Footer ===== */
        .site-footer{
            margin-top:auto; /* 항상 하단 */
            background:#e5e7eb;
            border-top:1px solid #d1d5db;
        }
        .footer-inner{
            max-width:var(--maxw); margin:0 auto;
            padding:16px 20px; color:#111827; font-size:14px;
        }
        .footer-row{ display:flex; flex-wrap:wrap; align-items:center; gap:8px; margin-bottom:6px; }
        .footer-row a{ color:#0d6efd; text-decoration:underline; }
        @media (max-width:480px){
            .login-header, .login-body{ padding:22px; }
        }
    </style>
</head>
<body>

<!-- ===== Header 유지 ===== -->
<header class="site-header">
    <div class="topbar">
        <a href="/" class="logo" aria-label="홈으로">
            <img src="/download/logo.png" alt="KDT" class="eeung-logo" style="width:50px; height:auto;" />
        </a>
        <div class="auth-links">
            <a href="/login">로그인</a>
            <span class="sep">|</span>
            <a href="/register">회원가입</a>
        </div>
    </div>

</header>

<main>
    <div class="login-card">
        <div class="login-header">
            <h3><i class="fa-solid fa-running me-2 text-primary"></i>로그인</h3>
            <p>스포츠 매칭 플랫폼</p>
        </div>

        <div class="login-body">
            <form action="/login" method="post" novalidate>
                <!-- 아이디 -->
                <div class="mb-3">
                    <label for="username" class="form-label">아이디</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fa-solid fa-user"></i></span>
                        <input type="text" class="form-control" id="username" name="username"
                               required placeholder="아이디를 입력하세요"
                               autocomplete="username" autofocus/>
                    </div>
                </div>

                <!-- 비밀번호 -->
                <div class="mb-2">
                    <label for="password" class="form-label">비밀번호</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fa-solid fa-lock"></i></span>
                        <input type="password" class="form-control" id="password" name="password"
                               required placeholder="비밀번호를 입력하세요" autocomplete="current-password"/>
                    </div>
                </div>

                <!-- 옵션/링크 -->
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="rememberMe" name="remember-me">
                        <label class="form-check-label" for="rememberMe">자동 로그인</label>
                    </div>
                    <a href="/find-id" class="text-decoration-none" style="font-size:.9em;">아이디 찾기</a>
                </div>

                <!-- 제출 -->
                <div class="d-grid mb-2">
                    <button type="submit" class="btn btn-login">
                        <i class="fa-solid fa-futbol me-2"></i>로그인
                    </button>
                </div>
            </form>

            <div class="d-flex justify-content-center gap-3 mt-3 links-row">
                <a href="/register"><i class="fa-solid fa-user-plus me-1"></i>회원가입</a>
                <a href="/main"><i class="fa-solid fa-house me-1"></i>홈으로</a>
            </div>
        </div>
    </div>
</main>

<footer class="site-footer">
    <div class="footer-inner">
        <div class="footer-row">
            <span class="logo sm"></span>
        </div>
        <div class="footer-row">
            041-123-4567 | <a href="mailto:contact@eeung-sports.com">contact@eeung-sports.com</a> |
            충남 천안시 동남구 백석대학로 1 (백석대 스포츠센터 3층)
        </div>
        <div class="footer-row">
            SNS:
            <a href="#">Instagram</a>
            <a href="#">Facebook</a>
            <a href="#">YouTube</a>
            <span>&nbsp;| © 2025 EEUNG. All rights reserved.</span>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
    function openSportWindow(name){
        alert(name + ' 메뉴는 준비 중입니다!');
    }

</script>
</body>
</html>
