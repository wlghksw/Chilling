<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0"/>
    <title>사용자 관리</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <style>
        .sidebar{min-height:100vh;background:linear-gradient(135deg,#667eea 0%,#764ba2 100%);}
        .sidebar .nav-link{color:rgba(255,255,255,.8);padding:15px 20px;border-radius:8px;margin:5px 0;transition:all .3s;}
        .sidebar .nav-link:hover{color:#fff;background:rgba(255,255,255,.1);}
        .sidebar .nav-link.active{background:rgba(255,255,255,.2);color:#fff;}
        .main-content{background-color:#f8f9fa;}
        .card{border:none;border-radius:15px;box-shadow:0 0.125rem 0.25rem rgba(0,0,0,.075);}
        .table{background:#fff;border-radius:10px;overflow:hidden;}
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        <nav class="col-md-3 col-lg-2 d-md-block sidebar collapse">
            <div class="position-sticky pt-3">
                <div class="text-center mb-4"><h4 class="text-white"><i class="fas fa-shield-alt"></i> 관리자 패널</h4></div>
                <ul class="nav flex-column">
                    <li class="nav-item"><a class="nav-link" href="/admin"><i class="fas fa-tachometer-alt me-2"></i>대시보드</a></li>
                    <li class="nav-item"><a class="nav-link" href="/admin/posts"><i class="fas fa-file-alt me-2"></i>게시글 관리</a></li>
                    <li class="nav-item"><a class="nav-link active" href="/admin/users"><i class="fas fa-users me-2"></i>사용자 관리</a></li>
                </ul>
            </div>
        </nav>

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 main-content">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">사용자 관리</h1>
                <div class="btn-group">
                    <a href="/" class="btn btn-outline-secondary btn-sm"><i class="fas fa-home me-1"></i>홈으로</a>
                    <form action="/logout" method="post" style="display:inline">
                        <button type="submit" class="btn btn-outline-danger btn-sm"><i class="fas fa-sign-out-alt me-1"></i>로그아웃</button>
                    </form>
                </div>
            </div>

            {{#successMessage}}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i><span>{{.}}</span>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {{/successMessage}}

            {{#errorMessage}}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i><span>{{.}}</span>
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {{/errorMessage}}

            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0"><i class="fas fa-users me-2"></i>사용자 목록
                        <span class="badge bg-primary ms-2">{{usersCount}}</span>
                    </h5>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-dark">
                            <tr><th>ID</th><th>사용자명</th><th>이메일</th><th>역할</th><th>상태</th><th>가입일</th><th>게시글 수</th><th>관리</th></tr>
                            </thead>
                            <tbody>
                            {{#users}}
                                <tr class="{{^isActive}}table-danger{{/isActive}}">
                                    <td>{{id}}</td>
                                    <td><strong>{{nickname}}</strong></td>
                                    <td>{{email}}</td>
                                    <td>
                                        {{#isAdmin}}<span class="badge bg-danger">관리자</span>{{/isAdmin}}
                                        {{^isAdmin}}<span class="badge bg-secondary">일반사용자</span>{{/isAdmin}}
                                    </td>
                                    <td>
                                        {{#isActive}}<span class="badge bg-success">활성</span>{{/isActive}}
                                        {{^isActive}}<span class="badge bg-danger">탈퇴</span>{{/isActive}}
                                    </td>
                                    <td>{{createdAt}}</td>
                                    <td><span class="badge bg-info">{{postCount}}</span></td>
                                    <td>
                                        <div class="btn-group" role="group">
                                            <button type="button" class="btn btn-warning btn-sm" data-bs-toggle="modal" data-bs-target="#roleModal{{id}}">
                                                <i class="fas fa-user-edit"></i> 역할변경
                                            </button>
                                            
                                            {{#isActive}}
                                                <form action="/admin/users/{{id}}/force-withdraw" method="post" style="display:inline">
                                                    <button type="submit" class="btn btn-warning btn-sm" onclick="return confirm('정말로 이 사용자를 강제 탈퇴 처리하시겠습니까?')">
                                                        <i class="fas fa-user-times"></i> 강제탈퇴
                                                    </button>
                                                </form>
                                            {{/isActive}}
                                            
                                            {{^isActive}}
                                                <form action="/admin/users/{{id}}/restore" method="post" style="display:inline">
                                                    <button type="submit" class="btn btn-success btn-sm" onclick="return confirm('정말로 이 사용자 계정을 복구하시겠습니까?')">
                                                        <i class="fas fa-user-check"></i> 계정복구
                                                    </button>
                                                </form>
                                            {{/isActive}}
                                            

                                        </div>
                                    </td>
                                </tr>
                            {{/users}}

                            {{^users}}
                                <tr>
                                    <td colspan="8" class="text-center py-4">
                                        <i class="fas fa-users fa-3x text-muted mb-3"></i><br/>
                                        <span class="text-muted">등록된 사용자가 없습니다.</span>
                                    </td>
                                </tr>
                            {{/users}}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </main>
    </div>
</div>

{{#users}}
    <div class="modal fade" id="roleModal{{id}}" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">사용자 역할 변경</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form action="/admin/users/{{id}}/role" method="post">
                    <div class="modal-body">
                        <p><strong>{{nickname}}</strong> 사용자의 역할을 변경합니다.</p>
                        <div class="mb-3">
                            <label for="role-{{id}}" class="form-label">역할 선택</label>
                            <select name="role" id="role-{{id}}" class="form-select">
                                <option value="USER" {{^isAdmin}}selected{{/isAdmin}}>일반사용자</option>
                                <option value="ADMIN" {{#isAdmin}}selected{{/isAdmin}}>관리자</option>
                            </select>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">취소</button>
                        <button type="submit" class="btn btn-warning">역할 변경</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{{/users}}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
